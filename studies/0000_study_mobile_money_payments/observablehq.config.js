// Observable Framework config. Shared theming, layout, footer/TOC handling, the
// parent-page embed bridge and the case-study callouts all live in
// ../_shared/observable.base.js.
import { defineStudy } from "../_shared/observable.base.js";

export default defineStudy({ title: "Payment is the product" });
