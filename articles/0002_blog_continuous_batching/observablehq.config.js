// Observable Framework config. Shared theming, layout, footer/TOC handling and
// the parent-page embed bridge all live in ../_shared/observable.base.js.
import { defineArticle } from "../_shared/observable.base.js";

export default defineArticle({ title: "Continuous batching in practice" });
