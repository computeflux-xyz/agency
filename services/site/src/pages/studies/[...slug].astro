---
// Case-study detail (root locale). Locale-aware logic lives in the shared
// component. The English route is src/pages/en/studies/[...slug].astro.
export const prerender = false;
import StudyDetail from "@organisms/StudyDetail.astro";

const slug = Astro.params.slug ?? "";
---

<StudyDetail slug={slug} />
