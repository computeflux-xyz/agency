---
// French articles index (/fr/articles). Same shared component as the English
// route. It resolves the locale from the URL and requests ?lang=fr from site-api.
export const prerender = false;
import ArticlesIndex from "@organisms/ArticlesIndex.astro";
---

<ArticlesIndex />
