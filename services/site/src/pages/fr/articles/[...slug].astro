---
// French article detail (/fr/articles/[slug]). Same shared component as the
// English route. It resolves the locale from the URL and requests ?lang=fr.
export const prerender = false;
import ArticleDetail from "@organisms/ArticleDetail.astro";

const slug = Astro.params.slug ?? "";
---

<ArticleDetail slug={slug} />
