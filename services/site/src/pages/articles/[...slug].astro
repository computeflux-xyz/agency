---
// English article detail (root). Locale-aware logic lives in the shared
// component. The English route is src/pages/en/articles/[...slug].astro.
export const prerender = false;
import ArticleDetail from "@organisms/ArticleDetail.astro";

const slug = Astro.params.slug ?? "";
---

<ArticleDetail slug={slug} />
