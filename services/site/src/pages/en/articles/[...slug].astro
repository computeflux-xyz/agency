---
// English article detail (/en/articles/[slug]). Same shared component as the
// French root route. It resolves the locale from the URL and requests ?lang=en.
export const prerender = false;
import ArticleDetail from "@organisms/ArticleDetail.astro";

const slug = Astro.params.slug ?? "";
---

<ArticleDetail slug={slug} />
